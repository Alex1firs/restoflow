{
  "//": "Test-only tsconfig. Aliases the `server-only` marker package to its no-op",
  "//2": "server entry so the AI layer can run under tsx/node outside Next.js.",
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "baseUrl": ".",
    "paths": {
      "server-only": ["../../../node_modules/server-only/empty.js"]
    }
  }
}
